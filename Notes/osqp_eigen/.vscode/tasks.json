{
  "version": "2.0.0",
  "tasks": [
     {
      "type": "shell",
      "label":"build", //建立一个编译task
      "command": "cd ${workspaceFolder}/build ;cmake .. ; make -j4",  //注意这里不能用 &&，要用分号隔开
      "args": []
    }
  ]
}